# sov 2.0.0

# sov 1.0.1

* Added `plot_sov_geometry()` for visualizing one- and two-dimensional SOV geometry and updated an internal function so that weighted votes are accumulated in direction -r_k.
