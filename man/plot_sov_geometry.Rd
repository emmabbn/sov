% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sov_geometry.R
\name{plot_sov_geometry}
\alias{plot_sov_geometry}
\title{R/plot_sov_geometry.R}
\usage{
plot_sov_geometry(
  ideals,
  normals = NULL,
  midpoints = NULL,
  label_values = NULL,
  digits = 3,
  main = NULL
)
}
\arguments{
\item{ideals}{numeric matrix (voters x dimensions), with 1 or 2 columns, i.e. dimensions = 1,2.}

\item{normals}{optional numeric matrix (rollcalls x dimensions).  Rows are RCs.}

\item{midpoints}{optional numeric matrix (rollcalls x dimensions).  Rows are RCs.
If both \code{normals} and \code{midpoints} are supplied, \code{normals} are used.}

\item{label_values}{optional numeric vector to display \emph{instead of} \code{labels}
(e.g., SOV or VS-SOV). Will be formatted with \code{digits} and mapped to
\code{rownames(ideals)} if named; otherwise assumed in row order.}

\item{digits}{integer digits for \code{label_values} formatting.}

\item{rc}{integer, which roll call (row) to draw from \code{normals}/\code{midpoints}.}

\item{labels}{optional character vector of point labels; default is rownames(ideals).}

\item{pch, }{cex, col aesthetics for points.}

\item{show_normal, }{show_cut logical; draw normal arrow and cut line.}
}
\description{
plots SOV/VS-SOV geometry (1D or 2D)
Base-graphics helper to visualize ideal points and either a normal vector
(and optional cut line) or a cut midpoint (with inferred cut line).
}
\details{
You can pass \code{label_values} (e.g., SOV/VS-SOV) to replace voter labels.
}
